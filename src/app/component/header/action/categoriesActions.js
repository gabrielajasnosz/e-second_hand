const prefix = "CATEGORIES";

const categoriesActions = {
    setCategories: `${prefix}_SET_CATEGORIES`,
    setSizes: `${prefix}_SET_SIZES`,
    setBrands: `${prefix}_SET_BRANDS`,
};

export default categoriesActions;
