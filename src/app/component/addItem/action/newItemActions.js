const prefix = "NEW_ITEM";

const newItemActions = {
    setName: `${prefix}_SET_NAME`,
    setDescription: `${prefix}_SET_DESCRIPTION`,
    setBrand: `${prefix}_SET_BRAND`,
    setCategory: `${prefix}_SET_CATEGORY`,
    setColor: `${prefix}_SET_COLOR`,
    setSize: `${prefix}_SET_SIZE`,
    setPrice: `${prefix}_SET_PRICE`,
    setType: `${prefix}_SET_TYPE`,
    setSex: `${prefix}_SET_SEX`,
};

export default newItemActions;
